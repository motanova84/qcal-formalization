# NOESIS Interoperability Contract

Formalization node for the shared QCAL/NOESIS state.

Canonical constants:

```text
f0 = 141.7001
fB = 0.00052
Psi_target = 0.999999
```

Formal interfaces should expose these values as explicit definitions/constants, while derived propositions retain their hypotheses. Cross-repository computational claims are referenced by commit and deterministic test vector rather than by narrative state alone.

The intended bridge is:

`adelic spectral data -> QCAL state -> PHOENIX reference -> effective field sector -> formal invariants`.
